#include <stdio.h>
#define N 9
void main(){
    int i;
    char input[N+3];
    printf("Dwste enan akeraio (mexri %d psifia):",N);
    fflush(stdout); 
    fgets(input, sizeof input, stdin);
    fflush(stdin); 
    
}